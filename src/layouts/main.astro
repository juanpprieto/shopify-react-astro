---
// src/layouts/BaseLayout.astro

const {title } = Astro.props;
const isSRR = import.meta.env.SSR
import CountProvider from '../context/CountProvider.jsx'
// import { TinaProvider, TinaCMS } from 'tinacms'
// console.log('isSRR', isSRR)
// const cms = isSRR
//   ? {}
//   : new TinaCMS()
console.log('Astro.props', Astro.props)
---
<!doctype html>
<html lang="en">
  <head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <!-- <title>Buy {collection.data.title}</title> -->
    <link rel="icon" type="image/svg+xml" href="/favicon.svg">
    <link rel="stylesheet" href="/style/global.css">
    <title>Example Layout: {title}</title>
  </head>
  <body>
    <!-- Adds a navigation bar to every page. -->
    <header>
      <h1>Shoperoni</h1>
      <h2>Shop for literally the best products in the world, right here.</h2>
      <nav>
        <a class="home" href="/">Go back</a>
        <a class="cart" href="https://github.com">Shopping Cart</a>
      </nav>
    </header>
    <main>
      <!-- <CountProvider test={'1234'}> -->
        <slot />
      <!-- </CountProvider> -->
    </main>
  </body>
</html>